module = DATA_BUG
module-str = data_buf
source "subsys/logging/Kconfig.template.log_config"


config DATA_BUF
	bool "Enable data buffers"
	help
	   Enable data buffers

config DATA_BUF_POOL_BYTES
	int "Size of the data buffer pool in bytes"
	default 10240
	help
	   Size of the data buffer pool in bytes